<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RecipeGenerator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary Brand Color. -->
        <item name="colorPrimary">@color/suave</item>
        <item name="colorPrimaryVariant">@color/beige</item>
        <item name="colorOnPrimary">@color/nude</item>

        <!-- Secondary Brand Color. -->
        <item name="colorSecondary">@color/grey</item>
        <item name="colorSecondaryVariant">@color/brook</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>


    </style>

    <style name="Theme.Theme.RecipeGenerator" parent="Base.Theme.RecipeGenerator" />
</resources>