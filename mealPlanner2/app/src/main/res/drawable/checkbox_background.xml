<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="#ebe3db" /> <!-- Light blue background -->

    <!-- Border color and width -->
    <stroke
        android:width="5dp"
        android:color="#3a2d28" /> <!-- Darker blue border -->

    <!-- Rounded corners -->
    <corners android:radius="8dp" />

    <!-- Padding inside the background -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />
</shape>
